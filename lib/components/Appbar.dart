import 'package:flutter/material.dart';

 class Appbar {
 static defaultAppbar() {
    return AppBar(
      backgroundColor: new Color(0xFFF6F2F9),
      title: Icon(Icons.arrow_back_ios,color: Colors.grey,)
        );
  }
}
